{$DOMAIN_NAME:localhost}, :80 {
    # Reverse proxy API requests to FastAPI backend
    handle /api/* {
        reverse_proxy backend:8000
    }

    # Reverse proxy all other web requests to React Frontend
    handle {
        reverse_proxy frontend:80
    }
}
